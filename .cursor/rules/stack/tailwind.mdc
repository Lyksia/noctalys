---
alwaysApply: false
description: Tailwind conventions (structure, classes, variants)
---

# Tailwind CSS

**Objective**: Standardize Tailwind CSS usage to ensure consistency, maintainability, and adherence to best practices.

**Context**: To apply for standardizing the use of this technology in projects.

## Explanation

Tailwind CSS is the CSS framework used for all projects. Prioritize **utility classes** and avoid inline CSS or custom CSS files except for global tokens.

**Key Principles:**

- **Utility-first**: use Tailwind classes rather than custom CSS.
- **No inline CSS**: always use Tailwind.
- **Centralized tokens**: define colors, spacing, and tokens in `tailwind.config.ts`.
- **Dark mode**: use the `dark:` prefix for dark theme.
- **Responsive**: use Tailwind breakpoints (`sm:`, `md:`, `lg:`, etc.).
- **Variants**: handle complex variants with `cva()` or `clsx()`.

**Configuration `tailwind.config.ts` :**

```ts
import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./app/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  darkMode: "class",
  theme: {
    extend: {
      colors: {
        primary: {
          50: "#f0f9ff",
          100: "#e0f2fe",
          // ...
          900: "#0c4a6e",
        },
      },
      spacing: {
        18: "4.5rem",
        112: "28rem",
      },
    },
  },
  plugins: [],
};

export default config;
```

## Practical Implementation Example

**Basic Tailwind classes:**

```tsx
// ✅ Good: Tailwind classes
export function Button({ children }: { children: React.ReactNode }) {
  return (
    <button className="px-4 py-2 bg-primary-500 text-white rounded-lg hover:bg-primary-600 transition-colors">
      {children}
    </button>
  );
}

// ❌ Bad: inline CSS
export function Button({ children }: { children: React.ReactNode }) {
  return (
    <button style={{ padding: "0.5rem 1rem", backgroundColor: "#3b82f6" }}>
      {children}
    </button>
  );
}
```

**Dark mode :**

```tsx
export function Card({ children }: { children: React.ReactNode }) {
  return (
    <div className="bg-white dark:bg-gray-800 text-gray-900 dark:text-gray-100 rounded-lg shadow-md p-6">
      {children}
    </div>
  );
}
```

**Responsive :**

```tsx
export function Hero() {
  return (
    <section className="container mx-auto px-4 sm:px-6 lg:px-8">
      <h1 className="text-2xl sm:text-4xl lg:text-6xl font-bold">
        Welcome to Lyksia
      </h1>
    </section>
  );
}
```

**Variants with `cva()`:**

```ts
// src/components/ui/button-variants.ts
import { cva } from "class-variance-authority";

export const buttonVariants = cva(
  "inline-flex items-center justify-center rounded-md font-medium transition-colors",
  {
    variants: {
      variant: {
        default: "bg-primary-500 text-white hover:bg-primary-600",
        outline: "border border-gray-300 bg-transparent hover:bg-gray-100",
        ghost: "hover:bg-gray-100",
      },
      size: {
        sm: "px-3 py-1.5 text-sm",
        md: "px-4 py-2 text-base",
        lg: "px-6 py-3 text-lg",
      },
    },
    defaultVariants: {
      variant: "default",
      size: "md",
    },
  }
);
```

**Usage with `clsx()`:**

```tsx
import { clsx } from "clsx";

interface ButtonProps {
  variant?: "primary" | "secondary";
  isLoading?: boolean;
}

export function Button({ variant = "primary", isLoading }: ButtonProps) {
  return (
    <button
      className={clsx(
        "px-4 py-2 rounded-lg font-medium transition-colors",
        variant === "primary" && "bg-blue-500 text-white hover:bg-blue-600",
        variant === "secondary" && "bg-gray-200 text-gray-900 hover:bg-gray-300",
        isLoading && "opacity-50 cursor-not-allowed"
      )}
    >
      {isLoading ? "Loading..." : "Click me"}
    </button>
  );
}
```

## Variants or Exceptions

- Complex animations may require custom CSS or Framer Motion.
- Existing projects in migration can temporarily mix Tailwind and classic CSS.

## Links / References

- [Tailwind CSS Documentation](https://tailwindcss.com/docs)
- [Class Variance Authority](https://cva.style/docs)
- [clsx Documentation](https://github.com/lukeed/clsx)
